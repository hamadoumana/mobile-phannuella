/// Noms des topics/canaux Firebase utilises par l'app.
///
/// firebase_options.dart est genere par `flutterfire configure` (non commite,
/// cf. .gitignore) et n'est donc pas encore present dans ce squelette.
class FirebaseConstants {
  FirebaseConstants._();

  static const String fcmDefaultChannelId = 'tailorshop_default_channel';
  static const String fcmDefaultChannelName = 'Notifications TailorShop';
}
